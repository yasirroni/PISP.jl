# Regenerable Documenter.jl site build; rebuild with:
#   julia --project=docs docs/make.jl
#
# Keep docs/src/generated/ committed. Those files are rendered Literate tutorial pages, not throwaway build output.
build/
