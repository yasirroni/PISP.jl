# Caveats

This page is kept as a compatibility alias. The current human-facing discussion is [Assumptions and scope](@ref), with source and mapping details split across [Data sources](@ref), [Output tables](@ref), and [Parameters and mappings](@ref).
